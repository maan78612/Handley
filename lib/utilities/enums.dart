enum BookingType {
  complete,
  pending,
  active,
  cancel
}


enum UserGender {
  male,
  female,

}

enum UserType {
  customer,
  professional,
}

enum MessageTypeEnums {
 text , image, file,pdf;
}
